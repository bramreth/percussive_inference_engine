percussive_inference_engine

to use, in a python environment, from the commandline run the main.py file with --help to view the arguments required. 
following is an example set of arguments for generating the audio file from the enter sandman test input:

test_input_wavs/sandman_test.wav -s structure/structure_sandman.yaml

executing the file yields new results in the beat file directory.

the provided virtual environment contains python 3.6 with all required pip installs already done.